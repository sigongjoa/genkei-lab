# genkei-lab — 피규어 원형(原型) 연구실

원화에서 **출력 키트**(부품 STL · 분해도 · 색별 출력)까지 가는 피규어 제작 프로그램을 만들며 배우는 저장소.
원칙은 셋이다.

- **도면이 중심이다.** 그림에는 수치가 없다. 사람이 고치는 것은 수치 도면이고, 3D 는 도면에서 계산된다.
- **GUI 와 스크립트는 같은 명령을 부른다.** 조작마다 명령 한 줄이 저널에 남고, 저널을 다시 틀면 같은 결과가 나와야 한다.
- **직접 지은 것만 믿는다.** 핵심 연산은 우리 코드로 짓고, 옮길 때는 옛 판을 정답기로 삼아 결과를 대 본다.

## 조각 (`조각/`) — 스컬프트 최소판

붓 다섯(그리기 · 부풀리기 · 매끈 · 잡기 · 주름) · 대칭 X · 반대로(파기) · 리메시 · 타임라인(되감기 · 타임랩스 · 갈래) · 스크립트 재생 검증.

| 파일 | 하는 일 |
|---|---|
| `engine.py` | 붓 계산 파이썬 판 — **정답기**. 붓 공식은 SculptGL 과 같다 |
| `cpp/sculpt.cpp` | 같은 계산의 C++ 판(DLL). 붓 닿은 곳만 계산해 21만 정점에서 자국당 0.6 ms |
| `engine_cpp.py` | C++ 판을 파이썬에서 부르고, 정답기와 정점 차이를 잰다 |
| `그림.py` | 그림 두 장 → 카탈로그 후보 → 입력 그림과 대 보고 고름(정답 모름) · 동점은 사람 몫. `조각.exe 만들기 앞 옆 키 폴더` |
| `키트.py` | 모양을 앞 그림 부위로 잘라 부품 · 몸통 쪽 끼움 핀(공차 0.15 mm) · 출력 판정 Q1~Q6 · 조립/분해 그림. `조각.exe 키트 앞 옆 키 폴더` |
| `dsl.py` | 조형 DSL — 상자 · 원기둥 · 원뿔 · 구 · 토러스 · 타원체 · 캡슐 · 반구 · 각뿔대 · 둥근상자 · 관 · 로프트를 + · - 로. 한 줄 = 저널 한 칸 (`API.DSL`) |
| `remesh.py` | 복셀 거리장 + 마칭 큐브로 고르게 다시 깔기 |
| `app.py` · `ui/` | 창(pywebview + three.js). 마우스를 **모델 위 좌표(mm)** 로 바꿔 엔진을 부른다 |

### 돌리기

```
pip install numpy scipy trimesh scikit-image pywebview manifold3d
python 조각/app.py              # 창
python 조각/engine.py 검사       # 결정성 · 재생 · 대칭 · 매끈 · 되감기
python 조각/engine_cpp.py        # 파이썬 판 = C++ 판 (최대 차이) · 속도
python 조각/remesh.py            # 리메시 결정성 · 모서리 고름 · 모양 유지
python 조각/app.py 점검          # 창 없이 API 한 바퀴 (갈래 · 리메시 뒤 재생 포함)
```

C++ DLL 다시 굽기 (MSVC):

```
cl /nologo /O2 /std:c++17 /utf-8 /EHsc /LD 조각/cpp/sculpt.cpp /Fe:조각/cpp/sculpt.dll
```

exe (PyInstaller):

```
cd 조각
python -m PyInstaller --noconfirm --onefile --windowed --name 조각 --add-data "ui;ui" --add-binary "cpp/sculpt.dll;cpp" --collect-submodules trimesh --collect-submodules skimage.measure --collect-submodules skimage.draw --hidden-import scipy.spatial --hidden-import manifold3d app.py
```

## 벤치 (`벤치/`)

조각 메시(mm)를 art2real `원화3d` 벤치(도형 11 · 마네킹 7 · 실물 28)의 자로 잰다. 채점 코드와 정답은 `원화3d` 에 있어 공개 저장소만으로는 안 돈다.

| 파일 | 하는 일 |
|---|---|
| `어댑터.py` | 좌표 맞추기(조각 정면 -y → 벤치 정면 +y · 키 1 로 앉힘) · 3D IoU(속 채움 두 규약) · 앞 · 옆 실루엣 IoU |
| `도형DSL.py` | DSL v0: 시트 그림 두 장에서 잰 수치로 기본형을 놓는다. 식별 가능 여덟 중앙 0.997 · 토러스 1.000 · 못 가르는 셋은 후보를 고른다 |
| `키트벤치.py` | `조각.exe 키트` 로 46장: 부품 · 핀 · 판정 Q1~Q6. 사람형 대부분 부품 6 · 핀 5, 부피 합 = 모양, 자립은 받침으로 46/46 |
| `도형2.py` | 새 기본형 여섯(타원체 · 캡슐 · 반구 · 각뿔대 · 둥근상자 · 관)을 새 정답 일곱으로 · 0.991~1.000. 원뿔 ↔ 사각뿔 동점은 화소 오차가 정한다 |
| `천장.py` | 그림 두 장의 천장: 사람형 중앙 H hull 0.662 · C 층 타원 0.745 · C + 참 깊이 0.802 — 이 계열의 끝은 0.80 근처 |
| `사람형.py` | 사람형 A 부위 타원뿔대 · B 부위 로프트 · C 층 타원을 exe 로. 사람형 35장 중앙 A 0.421 · B 0.568 · C 0.745 · exe 가 C 를 고름 |
| `exe벤치.py` | **조각.exe 를 그대로 부른다** — 그림 두 장만 주고 나온 파일을 채점 · 저널 재생 해시 확인. 도형은 잡고, 사람형은 중앙 0.33~0.37 |
| `대조군.py` | 도형 11 바닥 두 줄: 구 그대로(중앙 0.360) · 수축포장(정점만 옮긴 끝). 예측 먼저 적고 잰다 |

```
python 벤치/어댑터.py 검사     # 정답 왕복 12 장 = 1.000 · 축을 틀리면 떨어진다 -> 벤치/out/어댑터_검사.png
python 벤치/대조군.py          # -> 벤치/out/대조군.{json,png}
python 벤치/도형DSL.py         # -> 벤치/out/도형DSL.{json,png} (대조군을 먼저)
python 벤치/exe벤치.py         # 조각/dist/조각.exe 를 먼저 굽는다 -> 벤치/out/exe벤치.{json,png}
python 벤치/사람형.py          # -> 벤치/out/사람형.{json,png}
python 조각/dsl.py 검사         # DSL 닫힘 · 결정성 · 두 덩어리 · 구멍 · 모르는 식 거절
```

## 다음 (09-23 기준)

지금: 그림 두 장 → `조각.exe 키트` → 부품 STL · 핀 · 판정 Q1~Q6 · 조립/분해 그림. 46장 결과는 `벤치/키트벤치.py` 머리말.

**09-23 원형사 조사와 대 보니 놓친 것** (art2real `claudedocs/research_형태와분할_일본원형사_20260923.md`):
원형사는 한 덩어리를 자르지 않고 **겹으로 짓는다** — 누드 소체 → 두께 있는 옷 껍질 → 머리카락 → 소품. 부품 = 도색 단위(피부는 한 부품),
이음매는 덮이는 곳(소매 속 손 · 깃 밑 목 · 치마 속 다리), 다보는 네모 · 사다리꼴(돌지 않고 위아래가 보이게, 좌우 닮은 부품은 다르게),
틀에서 한 방향으로 안 빠지면 쪼갠다, 깊이는 소체 사전에서, 입력은 4면 설정화. 우리 키트는 한 덩어리를 겉면에서 잘랐다 —
마네킹 Q2(목 · 손목 < 3 mm)는 **자르면 안 되는 곳을 자른 것**이다(굵히기는 증상 처방).

1. ~~다보 모양~~ **됐다 (09-23)** — 사다리꼴(왼) · 뒤집힌 사다리꼴(오) · 좌우 크기는 큰 쪽으로. 판정 Q7 로 잰다:
   90° 돌리면 밖으로 0.14~0.22 · 왼 다보는 오른 구멍에 0.12~0.15 남는다. 핀 있는 35장 모두 통과.
   (네모는 4 겹 대칭이라 90° 돌려도 들어가 안 썼다. 크기가 다르면 작은 다보가 큰 구멍에 헐렁하게 들어가 크기를 맞췄다.)
**09-23 art2real 업데이트를 대 본 순서** (원형사.py · 제약분할.py · 조립.py · 지표.py · Printables 키트 정답 — `G:rt2real\원화3d\실험_20260923`):

2. **자 여럿을 키트 벤치에** — 챔퍼 · 새 각도 실루엣 · 표면각 · F-score@τ (art2real `지표.py` 를 어댑터에). IoU 하나로 그림 닮음을 판정하지 않는다.
   우리 층 타원 60 켜는 계단이라 표면각에서 잡힐 것(art2real: 우리 1.4° 대 정답 9.1° = 너무 매끈).
3. **빠짐 방향을 분할의 제약으로 (Q8)** — `제약분할.py`: 한 축으로 안 빠지면 빠질 때까지 쪼갠다. 피부 한 부품(목은 몸통) · 이음매는 덮이는 곳도 여기서.
4. **조립 순서 · 끼우는 방향** — `조립.py` 를 우리 부품에: 닿음 그래프 · 바닥부터 순서 · 방향 · 끼우다 막히는 곳. 키트.json 에 계획, 분해도는 그 순서로.
5. **분할 정답 = 실제 키트** — Printables CC-BY 다부품(34 · 30 · 28) · 인물 300+ 부품. shape mIoU + 맞은 짝으로 분할 품질을 잰다.
6. **겹 = 소체 사전 + 옷 껍질** — 표준 인체 메시를 정면 윤곽 · 관절에 맞춘다. art2real 결론: 깊이는 그림에서 안 나온다(20편 중 0 · DA3 0.55 · 사람이 완벽히 적어도 0.617), hull 깎은 소체는 무의미, GPT 는 겹을 못 그린다. 우리 혼합(팔 깊이 = 폭)이 그 초보판. 천장 0.80 을 넘을 유일한 길이고 가장 큰 일.
7. 전에 적은 것 — Q5 떠 있는 부품 · 사람 아닌 것은 한 부품 + 받침 · MCP 서버와 와이어프레임 화면.

**입력 결정 (09-23 사용자):** 서비스는 **그림 한 장**을 받고, 옆 · 뒤 · 위는 **AI 가 생성**한다. 생성 그림은 저장해 해시를 저널에(뒤는 결정적).
생성 시야는 원본과 키 · 폭 · 윤곽이 맞는지 검사해 어긋나면 버린다. 벤치는 「앞 한 장 + 생성」으로 다시 — 지금 수치는 생성이 완벽할 때의 천장.
생성기: 로컬 ComfyUI 에 다중 뷰 가중치가 없다 → 후보(Zero123++ · Era3D · MV-Adapter …)의 크기 · 라이선스 · VRAM 을 정리해 고른다(다운로드는 허락 뒤).
돌리는 법 — 벤치는 백그라운드에서 메모리가 모자라 죽는다. 포그라운드로 나눠서:
`python 벤치/키트벤치.py 이어서 10` 를 「N 장 남음」이 0 이 될 때까지, 끝나면 `python 벤치/키트벤치.py 판정`.
exe 를 다시 구운 뒤에는 `벤치/out/키트벤치.json` 을 지우고 처음부터 (한 벤치 = 한 exe 판).

## 외부 코드

- [three.js](https://threejs.org) r160 — MIT (`조각/ui/lib/LICENSE-three.txt`)
- 붓 공식은 [SculptGL](https://github.com/stephomi/sculptgl)(MIT)의 식을 따라 새로 썼다
